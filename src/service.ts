/** Defines `enum` for spell check services. */

/** Spell check service type. */
export enum SpellCheckService {
  // Pusan National University.
  pnu = 0,
  // Daum.
  daum,
  // All of them.
  all,
}
